# VAL-0008 — Tab Bars (iPhone pair dark; iPad frame out of scope)
Finding: seeded full-width tab-bar model is pre-26 era; kit shows floating pill
(Liquid Glass chrome). Height token survives (~50); container model amended:
inset/offset/radius tokens added. Active = inset segment + accent content.
First paradigm-level amendment. Regular-mode top bar still pending validation.
