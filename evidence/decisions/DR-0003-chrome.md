# DR-0003 — Chrome

**Decision:** chrome is in-page only

**Status:** owner decision

The system owns only in-page chrome: top bar, sidebar (regular), and tab bar
(compact). It does not own or render traffic lights, window metrics, or
drag regions — those belong to the browser or device, not this system.
